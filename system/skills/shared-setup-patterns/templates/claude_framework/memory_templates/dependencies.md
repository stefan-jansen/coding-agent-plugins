# Dependencies

Track project dependencies and their versions here.

## Package Manager Dependencies
See package.json, pyproject.toml, or requirements.txt for current dependency list.

## System Dependencies
List any system-level dependencies (databases, services, tools) required to run this project.

## Development Dependencies
Additional tools needed for development (linters, formatters, test runners).
